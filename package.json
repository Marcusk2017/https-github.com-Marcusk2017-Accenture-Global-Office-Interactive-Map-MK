{
  "name": "accenture-global-office-map",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/frontend",
    "apps/backend"
  ],
  "scripts": {
    "build": "npm run build -w apps/frontend && npm run build -w apps/backend",
    "dev": "concurrently \"npm run dev -w apps/backend\" \"npm run dev -w apps/frontend\"",
    "start": "npm run start -w apps/backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

